 #include<stdio.h>
 int main()
  {
    enum Days {Mon = 1,Tue,Wed,Thu,Fri,Sat,Sun};
    
     printf("%d \n",Mon);  // 1
     printf("%d \n",Tue);  // 2
     printf("%d \n",Wed);  
     printf("%d \n",Thu);  
     
     enum Days obj = Wed;
     printf("%d \n",obj);
     
     printf("Size of enum object %d \n",sizeof(obj));  // 4 byte
     
     enum result {false,true};  // 0  1
    
     enum gender {Male , Female};  // 0 1
     
     switch(choice)
      {
        case Mon : {}
      }
      
    return 0;
  }
